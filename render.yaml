services:
  - type: web
    name: predictive-execution-engine
    env: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api.main:app --host 0.0.0.0 --port 8000"
    autoDeploy: false

databases:
  - name: predictive-execution-db
    plan: free
